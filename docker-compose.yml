services:
  mysql:
    image: mysql
    environment:
      - MYSQL_DATABASE=db_example
      - MYSQL_PASSWORD=ThePassword
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_USER=springuser
    ports:
      - '3306:3306'
    expose:
      - "3306"